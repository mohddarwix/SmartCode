FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --legacy-peer-deps

COPY . .

EXPOSE 5173

# --host exposes the dev server to the Docker network so the mapped port works.
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
