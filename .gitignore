# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
dist-ssr
*.local

# env
.env
.env.local
.env.*.local

# backend
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.env

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# TLS certificates and private keys — should never be committed
*.pem
*.key

# database dump (contains real beta-tester emails + bcrypt hashes;
# the schema + seed files are the public source of truth and produce the
# same content deterministically)
database/dump.sql

# Internal / planning files — not part of the public project
.claude/
CODEBASE_EXPLAINED.md
PUBLISHING_PLAN.md

# Word lock files
~$*.docx
~$*.doc

# Old report sources (the canonical report is now SmartCode_Final_Report.pdf;
# the markdown source, Word renderings, and mermaid diagram sources are no
# longer the authoritative version and should not be re-added)
REPORT.md
REPORT*.docx
docs/*.mmd
